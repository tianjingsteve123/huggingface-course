# Huggingface Workspace

This folder is organized for clean GitHub repository standards:

- `course/` contains all runnable chapter scripts and Python dependencies.
- root level keeps only this README and the `course/` subfolder.
- no temporary model-export artifacts are kept in versioned files.

## Start Here

See `course/README.md` for setup, execution, and quality validation commands.
